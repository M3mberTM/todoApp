# TODO APP

The point of the application is to allow users to create todo tasks. These task can have different priorities and aren't shared between users. The tasks can also have deadlines and one task can be
connected to another.

## Backend

For backend, the app is using a simple express server along with MongoDB to save the data.

## Frontend

Frontend uses ReactJS along with an external library to add extra flare to the visuals.
